import { readFileSync, writeFileSync } from 'node:fs';
const env=Object.fromEntries(readFileSync('.env','utf8').split('\n').filter(l=>l.includes('=')).map(l=>{const i=l.indexOf('='); return [l.slice(0,i),l.slice(i+1).trim().replace(/^['"]|['"]$/g,'')]}));
const ids=process.argv.slice(2);
for(const id of ids){try{const r=await fetch(`https://gateway.thegraph.com/api/${env.THE_GRAPH_API_KEY}/subgraphs/id/${id}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({query:'{ _meta { block { number timestamp } hasIndexingErrors } __schema { types { name fields { name type { name kind ofType { name kind } } } } } }'})});const data=await r.json();writeFileSync(`output/benchmark/${id}.json`,JSON.stringify(data,null,2)); console.log(id,JSON.stringify(data.errors??data.data?._meta));for(const t of data.data?.__schema?.types??[])if(['Position','Account','LiquidityPosition','Collect','User'].includes(t.name))console.log(t.name,t.fields.map(f=>f.name).join(','));}catch{console.log(id,'failed')}}
