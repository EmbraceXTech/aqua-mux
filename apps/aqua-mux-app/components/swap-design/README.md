# Retired swap design files

`app/swap/design/page.tsx` renders the live `SwapView`, not this directory. These files are retained only as an unmounted design reference. They do not execute swaps.

The live integration is documented in `components/swap/README.md` and uses 1inch Classic Swap on Arbitrum.
